import { LoadingColorChangeDirective } from './loading-color-change.directive';

describe('LoadingColorChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingColorChangeDirective();
    expect(directive).toBeTruthy();
  });
});
